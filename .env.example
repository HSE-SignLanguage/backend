BACKEND_PORT=8080
ML_API_URL=http://localhost:9000/api/process
OPENROUTER_API_KEY=your_openrouter_api_key
OPENROUTER_MODEL=openrouter/your-model
OPENROUTER_SYSTEM_PROMPT=You turn literal sign-language transcripts into natural text. Extend the running transcript with the new literal chunk, keep grammar and language natural, and reply with the full updated transcript in Russian.
USE_OPENROUTER=true
